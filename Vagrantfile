Vagrant.configure("2") do |config|

  config.vm.box = "almalinux/10"
  config.vm.box_version = "10.1.20251125"

  config.vm.provider "libvirt" do |vb|
    vb.cpus = 2
    vb.memory = "2048"
  end
  
end